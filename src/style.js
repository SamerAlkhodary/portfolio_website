import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
	page: {
		width: '100%',
		position: 'relative',
	},
	
	body:{
		width: '100%',
		height: '100%',
	}

}));

export default useStyles;
